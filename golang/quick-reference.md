# Golang Quick Reference
```go
// bool to string
strconv.FormatBool(v)

// get type  
fmt.Printf("number: %v, type, %v", x, reflect.ValueOf(x).Type())
fmt.Printf("number: %v, type, %v", x, reflect.TypeOf(x))

// anything to string
fmt.Sprintf("%v", x)

```
